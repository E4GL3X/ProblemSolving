#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;
    for(int i = 0; i < t; i++) {
        int n, m;
        long long x, y;
        cin >> n >> m >> x >> y;
        for(int j = 0; j < n; j++) {
            long long temp;
            cin >> temp;
        }
        for(int j = 0; j < m; j++) {
            long long temp;
            cin >> temp;
        }
        cout << n + m << endl;
    }
    return 0;
}