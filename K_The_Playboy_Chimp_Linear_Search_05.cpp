#include<bits/stdc++.h>

using namespace std;
int main(){

    int n,q;
    cin >> n; 
    int arr[n];
    for (int i = 0; i < n; i++)
    {
        cin >> arr[i];
    }
    cin >> q;
    int newarr[q];
    for (int j=0 ; j<q ; j++){
        cin >> newarr[j];
    }

    for (int k=0 ; k<q ; k++){
        for (int l=0 ; l<n ; l++){
            
        }
    }
    
}